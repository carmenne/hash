#include <stdlib.h>
#include <stdio.h>
#include "util.h"

int main(int argc, char *argv[])
{
	DIE(argc < 1, "Please enter the initial length of hashtable:\n");
	
	int size = atoi(argv[1]);
	
	// Create the hashtable
	bucket hashtable[size] = calloc(size, sizeof(bucket*));
}